{
  "version": 2,
  "builds": [
    {
      "src": "index.html",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "env": {
    "POSTGRES_URL": "${POSTGRES_URL}",
    "POSTGRES_URL_NON_POOLING": "${POSTGRES_URL_NON_POOLING}"
  }
}
